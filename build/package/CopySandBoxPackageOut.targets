<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="14.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
    <ItemGroup>
        <_SandBoxPackageOutputFile Include="$(SdkInstallerFile)" />
    </ItemGroup>

    <Target Name="SetupPathDeb" Condition="'$(IsDebianBaseDistro)' == 'true'" DependsOnTargets="SetupDebProps">
    </Target>

    <Target Name="SetupPathRpm" Condition=" '$(IsRPMBasedDistro)' == true " DependsOnTargets="SetupRpmProps">
    </Target>

    <Target Name="CopySandBoxPackageOut" DependsOnTargets="SetupPathDeb;SetupPathRpm">
      <Copy
      SourceFiles="@(_SandBoxPackageOutputFile)"
      DestinationFolder="$(RepoRoot)/$(RelativeSandBoxPackageOutputFolder)"/>
    </Target>

</Project>
